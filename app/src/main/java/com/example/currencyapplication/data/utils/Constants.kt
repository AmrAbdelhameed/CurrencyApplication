package com.example.currencyapplication.data.utils

object Constants {
    object NetworkEndpoints {
        const val CURRENCY_LATEST: String = "/api/latest"
    }

    object Database {
        const val DATABASE_NAME: String = "currencies.db"
    }
}